etime='02:34'
echo "etime = $etime"
echo

# calculate expired time
min=$(echo $etime | awk -F: '{print $(NF-1)}')
echo "min = $min"
hour=$(echo $etime | awk -F: '{print $(NF-2)}')
echo "hour = $hour"

if [ $min -ge 05 ] || [ -n $hour ];
then
  echo "minutes greater than 05, or hour not null"
fi

